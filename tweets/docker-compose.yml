services:
  twitter:
    build: .
    platform: linux/amd64
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
  mongo:
    image: mongo
    ports:
      - "27017:27017"
